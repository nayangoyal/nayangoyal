#include<stdio.h>

int main(){

    int a;
    printf("Enter a value of a\n");
    scanf("%d", &a);
    if (a%2==0 && a>=2)
    {
        printf("NO");
    }else
    {
       printf("YES");
    }
        
    
        return 0;
}
